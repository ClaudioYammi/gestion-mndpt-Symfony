{% extends 'base.html.twig' %}

{% block title %}Etatvoiture index{% endblock %}

{% block body %}
    <h1>Etatvoiture index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numetat</th>
                <th>Etat</th>
                <th>Dateetat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etatvoiture in etatvoitures %}
            <tr>
                <td>{{ etatvoiture.id }}</td>
                <td>{{ etatvoiture.numetat }}</td>
                <td>{{ etatvoiture.etat }}</td>
                <td>{{ etatvoiture.dateetat ? etatvoiture.dateetat|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_etatvoiture_show', {'id': etatvoiture.id}) }}">show</a>
                    <a href="{{ path('app_etatvoiture_edit', {'id': etatvoiture.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_etatvoiture_new') }}">Create new</a>
{% endblock %}
