<form method="post" action="{{ path('app_detentionvoiture_delete', {'id': detentionvoiture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detentionvoiture.id) }}">
    <button class="btn">Delete</button>
</form>
