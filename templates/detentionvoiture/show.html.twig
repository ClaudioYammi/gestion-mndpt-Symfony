{% extends 'base.html.twig' %}

{% block title %}Detentionvoiture{% endblock %}

{% block body %}
    <h1>Detentionvoiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detentionvoiture.id }}</td>
            </tr>
            <tr>
                <th>Numdetention</th>
                <td>{{ detentionvoiture.numdetention }}</td>
            </tr>
            <tr>
                <th>Datedetention</th>
                <td>{{ detentionvoiture.datedetention ? detentionvoiture.datedetention|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_detentionvoiture_index') }}">back to list</a>

    <a href="{{ path('app_detentionvoiture_edit', {'id': detentionvoiture.id}) }}">edit</a>

    {{ include('detentionvoiture/_delete_form.html.twig') }}
{% endblock %}
