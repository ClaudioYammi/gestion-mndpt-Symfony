{% extends 'base.html.twig' %}

{% block title %}Detentionvoiture index{% endblock %}

{% block body %}
    <h1>Detentionvoiture index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numdetention</th>
                <th>Datedetention</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for detentionvoiture in detentionvoitures %}
            <tr>
                <td>{{ detentionvoiture.id }}</td>
                <td>{{ detentionvoiture.numdetention }}</td>
                <td>{{ detentionvoiture.datedetention ? detentionvoiture.datedetention|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_detentionvoiture_show', {'id': detentionvoiture.id}) }}">show</a>
                    <a href="{{ path('app_detentionvoiture_edit', {'id': detentionvoiture.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_detentionvoiture_new') }}">Create new</a>
{% endblock %}
